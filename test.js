import { describe, it } from "node:test";
import { strict, strictEqual } from "node:assert";

describe("strip-markdown", () => {
  it("should strip markdown", async () => {
    // TODO: Write tests
    await import("./index.js");
  });
});
